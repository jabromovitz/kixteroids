package enemies
{
	import starling.textures.Texture;
	import starling.events.Event;
	
	public class Ufo extends MovingObject
	{
		public function Ufo(texture:Texture)
		{
			super(texture);
		}
		
		override protected function init(e:Event):void
		{
			
		}
	}
}